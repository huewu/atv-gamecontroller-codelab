<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codelab.android.gamecontroller"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- step 1.4: TODO: set reqired="false" for H/W features not supported by Android TV -->
    <!--
        <uses-feature android:name="..." android:required="false"/>
    -->

    <!-- step 1.2: TODO: add @drawable/banner as a home screen banner -->
    <!-- step 1.3: TODO: notify this app is a game by setting isGame to be "true" -->
    <!-- step 1.5: TODO: Configure the activity to handle input device’s reconnection events -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".FullscreenActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- step 1.1: TODO add CATEGORY_LEANBACK_LAUNCHER here -->
            </intent-filter>
        </activity>
    </application>
</manifest>
